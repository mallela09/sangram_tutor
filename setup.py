from setuptools import setup, find_packages

setup(
    name="sangram_tutor",  # Make sure this matches your directory name
    version="0.1.0",
    packages=find_packages(),  # This should find your package correctly
    # ... other setup arguments
)
